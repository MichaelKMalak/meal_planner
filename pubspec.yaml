name: meal_planner
description: Meal Planner

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # L10n
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0

  google_fonts: ^2.1.0
  get_it: ^7.1.4
  provider: ^5.0.0

flutter:
  generate: true
  uses-material-design: true
