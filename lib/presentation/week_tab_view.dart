import 'package:flutter/material.dart';

class WeekTabView extends StatelessWidget {
  const WeekTabView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Week Tab View');
  }
}
